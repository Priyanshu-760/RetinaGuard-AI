# RetinaGuard runtime (Python 3.10, Windows). Install: pip install -r requirements.txt
tensorflow-cpu      # grading + UNet inference + Grad-CAM (app.py)
numpy           # image math, masks, overlays
Pillow          # image IO, enhancement, quality blur metric
flask           # web API (/, /health, /analyze, /report, /feedback, /validation)
matplotlib      # Grad-CAM JET colormap rendering (app.py)
reportlab       # vector PDF clinical report generation (services/pdf_report.py)
pandas          # offline validation scripts (optional at runtime)
gunicorn
